import { Opts } from './interfaces/config';
import { Rss } from './interfaces/rss';
import { Food } from './interfaces/food';
export declare class Easy {
    private _feed;
    private _RSS;
    private _locale;
    private _category;
    private _search;
    private _page;
    private _limit;
    private _contentLength;
    constructor(options: Opts);
    getFeed(): Promise<Array<Food>>;
    set RSS(rss: Rss[]);
    set locale(locale: string);
    set category(category: string);
    set search(search: string);
    set page(page: number);
    set limit(limit: number);
    set contentLength(contentLength: number);
    get RSS(): Rss[];
    get locale(): string;
    get category(): string;
    get search(): string;
    get page(): number;
    get limit(): number;
    get contentLength(): number;
}
